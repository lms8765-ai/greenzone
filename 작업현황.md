# 포항시 녹지 관리구역 시스템 — 작업 현황

## 개요
- 배포 주소: https://lms8765-ai.github.io/greenzone
- 저장소: https://github.com/lms8765-ai/greenzone (branch: main)
- 앱 본체: `index.html` (단일 파일, GitHub에 그대로 업로드하면 배포됨)
- 클라우드 저장: GitHub `data.json` (Personal Access Token 필요, 기기별로 ⚙️ 동기화 설정에서 입력)

## 🚨 진행 중 — 데이터 복구 (최우선)
2026-08-12 자동 동기화 로직 버그로 클라우드(data.json)가 샘플 데이터(3개 구역)로
덮어써지는 사고 발생. 원본 237개 구역 데이터는 사무실 PC 브라우저의 localStorage에
그대로 남아있는 것을 확인했고, 아래 절차로 백업 파일까지 이미 받아둔 상태.

- 백업 파일: 사무실 PC 다운로드 폴더의 `포항녹지_백업.json` (237개 구역 확인됨)
- 남은 절차: 그 PC(또는 파일을 옮긴 PC)에서 앱 상단 **📂 불러오기**로 이 파일을
  불러온 뒤 **☁️ 클라우드 저장**을 누르면 복구 완료. 불러온 직후 샘플 구역 3개
  (영일대 해변 장미길 / 인덕산 장미원 / 우현학산 장미특화거리)가 같이 보이면 삭제.
- 사고 원인: 여러 기기가 동시에 저장을 시도할 때 생기는 충돌을 자동 재시도하도록
  바꾼 코드가, 하필 빈/샘플 데이터를 들고 있던 기기의 재시도를 결국 성공시켜
  클라우드를 덮어씀. **재발 방지 안전장치를 이미 코드에 추가·배포 완료**:
  클라우드 대비 구역 수가 60% 미만으로 급감하면 자동/수동 저장·불러오기 모두
  경고 후 확인을 받도록 막아둠 (`isSuspiciousShrink` 함수, `saveToGitHub`/
  `loadFromGitHub`/`loadFromGitHubSafe` 전체 적용).

## 최근 완료된 기능
- 지적경계(연속지적도) 표시/숨김 토글 — 브이월드 데이터API, 화면 이동 시 자동 갱신
- 지번 경계 자동 불러오기 — 브이월드 PNU 정확매칭 (주소 검색 → 필지 경계를 구역으로 자동 생성)
- 월간보고서에 풀베기사업/초화식재 작업 현황 섹션 추가
- 1회성 마이그레이션 도구, 50m 사각형 자동생성 기능 삭제(정리)
- GitHub 클라우드 동기화 안정성 개선: "🔄 최신 불러오기" 수동 버튼 추가, 자동저장
  실패 시 재시도, 데이터 급감 안전장치(위 사고 이후 추가)
- 녹지관리 탭: 구역 라벨 기본 숨김, 선택 시에만 표시
- **구역별 수량 관리 (신규)**:
  - 구역 편집 모달의 담당자/작업자/다음 작업 예정일/예정 작업유형/작업유형 항목을
    "🔧 담당자·작업유형·예정일 더보기" 토글로 접어서 기본 숨김 처리
  - 구역별 입력 항목 추가: 풀베기 면적(㎡), 교목 수종별 수량(주, 여러 수종 등록 가능),
    관목 수종별 수량(주), 시설물 개수(EA)
  - 수종은 기존 수목관리 탭의 "수종 사전"(교목/관목 목록)을 그대로 재사용해 선택형으로
    입력 — 표기 방식이 자동으로 통일됨
  - 📊 현황 통계 탭의 "그룹별 구역 현황"에서 그룹 클릭 시 총 면적·풀베기 면적·
    교목/관목 수종별 합계·시설물 합계를 펼쳐볼 수 있음
  - "🌳 시 전체 수종 총계" 카드 추가 — 그룹·탭 구분 없이 전체 구역 합산, 엑셀 내보내기 포함
- **동기화 상태 표시 정확도 개선**: "토큰 확인 필요" 문구가 실제 토큰 문제가 아닌
  경우(안전장치 차단, 네트워크 오류, 연동 해제 등)에도 잘못 표시되던 문제 수정.
  상태를 error(진짜 토큰 문제)/blocked(안전장치 차단)/syncerror(기타 오류)로 분리.
  모바일 등 느린 네트워크에서 응답이 없을 때 12초 후 자동 타임아웃 처리 추가.
- **🔒 읽기 전용 잠금 (신규)**:
  - 앱은 기본적으로 읽기 전용 — 구역/작업기록/민원/장미관리 등 모든 데이터 변경이 차단됨
  - 우측 상단 "🔒 읽기 전용 (클릭: 편집)" 버튼 → 비밀번호 입력 시에만 편집 가능
  - 최초 사용 시 비밀번호를 직접 설정 (이후 모든 기기가 클라우드를 통해 같은 비밀번호 공유)
  - 잠금 해제 상태는 그 브라우저 탭/창을 닫으면 자동으로 다시 잠김
  - ⚠️ 참고: 서버 없는 정적 사이트 구조상 완전한 보안은 아니며, 실수/무단 편집 방지용
    안전장치입니다. 개발자도구를 다룰 줄 아는 사람의 의도적 우회까지는 막지 못합니다.

- **전체 오류 점검 및 잠금 우회 지점 보완 (신규)**:
  - 코드 전체 구문 검사(node --check), 태그 짝 검사, 버튼-함수 연결 무결성(365개 onclick 전수
    확인) — 이상 없음 확인
  - ⚠️ **중대 발견 및 수정**: 직전에 배포한 "읽기 전용 잠금"이 일부 기능에는 적용되지 않고
    있었음. 특히 "☁️ 클라우드 저장" 버튼과 30초 자동 동기화가 잠금 여부와 무관하게 항상
    작동하고 있어서, 잠금 상태에서도 실수로 바뀐 내용이 클라우드(모든 기기 공유)에 그대로
    올라갈 수 있는 허점이 있었음 → **가장 시급하게 수정 완료**
  - 그 외에도 그룹 추가/삭제, 부서·작업유형 관리, 민원 삭제, 장미구역 삭제, 작업묶음
    추가/삭제, 구역 숨김·고정, 지번 경계로 새 구역 만들기, 수목 도면(수목관리 탭) 저장 등
    39곳의 데이터 변경 기능에 잠금 확인이 빠져있던 것을 찾아 전부 추가함. (기존에는 구역
    저장/삭제, 민원 등록, 장미구역 등록 등 6곳만 적용되어 있었음)
  - 이제 읽기 전용 상태에서는 위 기능들을 실행하면 "🔒 읽기 전용 모드입니다" 안내만 뜨고
    실제로는 아무것도 저장되지 않음. "☁️ 클라우드 저장", "📂 불러오기" 버튼도 잠금 상태에서는
    회색으로 비활성 표시되도록 함
  - 참고: 이 잠금은 여전히 "실수 방지용" 이며, 개발자도구를 이용한 의도적 우회까지 막는
    완전한 보안은 아님 (기존과 동일)

- **구역별 수량 관리 개선 (신규)**:
  - 우측 상세 패널(구역 클릭 시)의 "작업 기록" 바로 위에 "📊 구역별 수량 관리" 요약이
    보이도록 추가 — 풀베기 면적·시설물·교목/관목 수종별 수량을 뱃지로 표시 (입력값이
    없는 구역은 이 섹션 자체가 나타나지 않음)
  - 관목 수량 단위를 "주" → "㎡"로 전면 변경 (입력폼, 상세패널, 그룹별/시 전체 통계,
    엑셀 내보내기까지 전부 반영). 교목은 기존대로 "주" 유지
  - 수종 입력 방식을 선택박스(select)로 개선 — 한 번이라도 입력된 수종은 드롭다운에서
    바로 골라 선택할 수 있고, 목록에 없는 새 수종은 "✏️ 새 수종 직접 입력"을 선택해
    입력. 브라우저 자동완성(datalist)에만 의존하지 않아 휴대폰에서도 안정적으로 동작
  - 위 변경 후 전체 코드 재점검(구문 검사, 태그 짝, 버튼-함수 연결, 잠금 가드 46건)
    모두 이상 없음 확인

- **수량관리 화면 정리 + 구역 병합 기능 (신규)**:
  - 상세 패널의 "📊 구역별 수량 관리"를 범례(legend)처럼 재정리 — 풀베기 면적/시설물은
    상단 요약 카드로, 교목/관목은 "N종 · 합계" 헤더 + 줄무늬(zebra) 표 형태로 표시해
    수량이 많아도 한눈에 훑어보기 쉽게 개선
  - **구역 병합 기능 추가**: 좌측 구역 목록에서 "여러 개 선택"으로 2개 이상의 구역을
    고른 뒤 하단 바의 "🔗 구역 병합" 버튼을 누르면, 선택한 구역들의 도형을 하나의
    구역 아래 "여러 구간"으로 합치고 풀베기 면적·교목/관목 수량·시설물·작업 기록까지
    전부 합산해 하나의 구역으로 만듦 (나머지 구역은 병합 후 자동 삭제). 기준 이름/그룹
    등 기본 정보는 선택 목록 중 첫 번째 구역을 따르며, 병합 전 팝업에서 이름을 바꿀 수 있음
  - 위 변경 포함 전체 코드 재점검(구문 검사, 태그/괄호 짝, 버튼-함수 연결, 잠금 가드
    49건) 모두 이상 없음 확인

- **📱 모바일(홈 화면 추가 앱) "옛날 데이터만 보임" 문제 수정**:
  - 원인: 홈 화면에 추가해서 쓰는 앱(PWA)은 아이콘을 다시 눌러도 새로고침되지 않고
    백그라운드에 있던 화면을 그대로 다시 보여주는 경우가 많음. 그런데 배경에 있는 동안은
    30초 자동 동기화 타이머도 운영체제가 멈춰버려서, 다시 켰을 때 다른 기기에서 바뀐
    최신 내용을 놓치고 예전 화면이 계속 보이는 문제였음
  - 조치: 앱이 다시 화면에 보일 때마다(백그라운드 → 화면 복귀) 최신 클라우드 데이터를
    즉시 자동으로 한 번 더 확인하도록 함. 오래 방치되어 지도가 깨져 보이는 경우를 대비해
    지도 크기도 함께 다시 계산하도록 함
  - 이 문제는 코드만으로 100% 재현하기 어려워, 배포 후 실제 휴대폰(홈 화면 앱)에서
    한동안 안 쓰다가 다시 켰을 때 최신 정보로 바로 바뀌는지 꼭 확인 부탁드립니다.
    (반영이 안 되면 정확한 화면을 다시 알려주세요)

- **최종 오류 점검 및 안정성 보완**:
  - 전체 구문 검사·태그/괄호 짝·버튼-함수 연결(중복 함수명 없음 포함) 재확인 — 이상 없음
  - 잠금 가드 49건 전부 정상 유지 확인 (구역 병합·범례 등 최근 추가 기능 포함)
  - ⚠️ **발견 및 수정**: 구역을 삭제하거나 병합할 때, 그 구역이 "작업묶음"이나
    "장미구역 상세정보"에 연결되어 있으면 그 연결 정보가 고아 참조로 남아있던 문제 수정
    - 구역 삭제 시: 작업묶음 목록에서도 자동으로 빠지고, 숨김/고정 표시도 함께 정리됨
      (장미구역 상세정보는 삭제하지 않고 연결만 해제 — 데이터 자체는 보존)
    - 구역 병합 시: 흡수되는 구역들이 속해있던 작업묶음은 자동으로 기준 구역 쪽으로
      옮겨지고, 장미구역 상세정보도 기준 구역에 없으면 자동으로 이어받음 (기준 구역에
      이미 있으면 충돌을 피하기 위해 연결만 해제하고 안내 메시지로 알려줌 — 데이터는 보존)

- **📱 모바일 "데이터 적용 중 오류 / 옛날 데이터만 보임" 근본 원인 수정**:
  - 보내주신 스크린샷("데이터를 불러왔지만 적용 중 오류", "연동 성공! 첫 사용이면 업로드하세요")
    을 근거로 실제 코드 버그 2가지를 찾아 수정함
  - ① **핵심 버그**: 클라우드에서 최신 데이터를 받아도, 이 기기에 저장된 "마지막 동기화
    시각" 기록이 최신이라고 (잘못) 판단되면 실제로는 화면에 구역이 하나도 없는데도
    "이미 최신 상태"라며 데이터를 반영하지 않던 문제. → 이제는 시각과 무관하게, 이
    기기의 화면 구역 수가 클라우드보다 적으면 무조건 최신 데이터를 반영하도록 수정
  - ② 구역 데이터 중 하나라도 옛날 형식이거나 좌표가 깨져 있으면(가져오기/병합 등으로
    드물게 발생 가능) 그 구역 하나 때문에 전체 화면 갱신이 통째로 멈춰버리던 문제 수정
    → 문제가 있는 구역은 건너뛰고 나머지는 정상 표시되도록 안전장치 추가
  - ③ 오류 발생 시 원인을 알 수 없이 "새로고침 해주세요"만 나오던 안내 문구에 실제
    오류 내용을 함께 표시하도록 개선 (다음에 또 발생하면 화면 문구만으로 바로 원인 파악 가능)
  - 위 변경 포함 전체 재점검(구문 검사, 태그/괄호 짝, 버튼-함수 연결, 잠금 가드 49건)
    모두 이상 없음 확인. 배포 후 문제가 재발하면 화면에 표시되는 정확한 문구를 캡처해서
    알려주시면 바로 원인 파악이 가능합니다.

- **"Unexpected end of JSON input" 오류 근본 수정 (콘솔 로그로 정확한 원인 확인)**:
  - 보내주신 개발자도구(F12) 콘솔 로그 덕분에 정확한 원인을 찾음: 클라우드에서 받은
    데이터를 다시 문서로 해석(JSON.parse)하는 단계에서 드물게 실패하는 문제였음
  - `sw.js`(오프라인 캐시 담당 파일)가 GitHub API 요청까지 "캐시해도 되는 일반 파일"
    경로로 잘못 처리하고 있던 부분을 수정 — 이제 GitHub API·지도 API 요청은 캐시를
    완전히 건너뛰고 네트워크로 직접 처리하도록 함 (`sw.js`도 함께 첨부하니 index.html과
    같이 GitHub에 업로드해주세요)
  - 데이터 디코딩이 혹시 한 번 실패하더라도 바로 포기하지 않고 자동으로 한 번 더
    새로 받아 재시도하도록 안전장치 추가
  - ⚠️ 확인 결과 클라우드(GitHub)에는 아직 처음 샘플 데이터(3개 구역)만 있고, PC
    화면의 240개 구역은 아직 클라우드로 올라가지 않은 상태입니다. 위 수정을 배포한
    뒤 PC(240개 구역이 보이는 그 화면)에서 "☁️ 클라우드 저장"을 한 번 눌러
    최종적으로 클라우드에 반영해주세요 — 그래야 휴대폰 등 다른 기기에서도 보입니다.

## 다음 작업 — 요청 대기
- 구간(떨어진 위치)별 수종·수량 별도 입력은 현재 보류 (구역 전체 합계로 충분하다고 판단)
- 수목관리(도면) 탭 내부의 세부 편집 기능(나무 1그루 옮기기 등 극히 세부적인 동작)은
  이번 점검에서 "저장" 단계는 모두 잠갔으나, 개별 클릭 동작 하나하나까지 잠그지는 않음 —
  저장이 막혀있어 실제 데이터 유실 위험은 없음

## 참고
- `vworld-proxy-worker.js`: 초기에 CORS 우회용으로 만든 Cloudflare Worker 스크립트.
  이후 JSONP 방식으로 대체되어 더 이상 필요 없음 (참고용으로만 보관).
- `지번경계기능_작업가이드.md`: 브이월드 키 발급 관련 사용자 작업 가이드(과거 작성분).
