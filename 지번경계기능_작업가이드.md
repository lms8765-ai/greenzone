# 지번 경계 자동 불러오기 — 작업 가이드

> 1회성 마이그레이션 도구는 코드에서 완전히 제거했습니다(헤더 버튼 + 관련 함수 2개). 검증 완료.

## 1. 제가(Claude) 이미 한 작업

- `createZoneFromParcel()` 함수 구현 — 브이월드 연속지적도(LP_PA_CBND_BUBUN) 조회 → 필지 경계 좌표를 구역 폴리곤으로 자동 변환
- 주소 검색 결과창(사이드 패널 + 지도 InfoWindow) 양쪽에 "📐 지번 경계 불러오기" 버튼 추가
- 브이월드 키 미보유 시 발급 안내 팝업 자동 표시
- 1회성 마이그레이션 도구(버튼 + 함수) 삭제
- 매 수정마다 `node --check` 구문 검사, div 태그 균형, 함수 중복 여부 확인

## 2. 사용자님이 해주셔야 하는 작업 (계정 소유자만 가능 — 제가 대신 할 수 없음)

| 순서 | 작업 | 위치 |
|---|---|---|
| 1 | 브이월드(vworld.kr) 로그인 → 마이페이지 → 오픈API 인증키 관리 | www.vworld.kr |
| 2 | 기존 키(도로망 참조용) 선택 → 활용 API 항목에 **"2D 데이터API"** 체크 추가 | 인증키 상세/수정 화면 |
| 3 | 활용 도메인에 `lms8765-ai.github.io` 등록되어 있는지 확인 (없으면 추가) | 같은 화면 |
| 4 | 이번에 받은 `index.html`을 GitHub에 업로드 (드래그 → Commit) | https://github.com/lms8765-ai/greenzone/upload/main |
| 5 | 배포된 앱에서 테스트: 주소 검색 → 검색 결과 핀 클릭 → **"📐 지번 경계 불러오기"** 클릭 | 배포 URL |
| 6 | 정상 동작 확인 시: 실제 관리구역 몇 곳에 적용해보고 경계가 지적도와 맞는지 확인 | — |
| 7 | 오류 발생 시(특히 CORS 관련): 브라우저 개발자도구(F12) → Console 탭 화면을 캡처해서 전달 | — |

## 3. 참고 — 미해결로 남아있는 항목

인수인계 문서 기준 아래 항목은 아직 손대지 않았습니다. 필요하실 때 말씀해주시면 진행하겠습니다.

- 예정일 표시 아이콘이 mowing/flower 탭일 때 🌿로 잘못 표시되는 사소한 이슈 (기능엔 지장 없음)
